"""cameraboi_cv — deterministic CV tools behind cameraBoi.

Precision (mm measurement from a fiducial mat + calibrated lens) and
exactness at scale (object counting, batch document scan preprocessing).
Capture stays in scripts/cameraboi; these tools consume image files.
"""

__version__ = "0.1.0"
